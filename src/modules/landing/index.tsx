import * as React from "react";
import styled from "@emotion/styled";

import Hero from "./components/Hero";

function Home() {
  return (
    <React.Fragment>
      <Hero />
    </React.Fragment>
  );
}

export default Home;
